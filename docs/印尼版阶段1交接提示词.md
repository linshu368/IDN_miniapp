# 印尼版阶段 1 · 新窗口交接提示词

把下面「提示词」整段粘贴到新 Agent 窗口。工作区请打开 **`/Users/qj/python_project/IDN_miniapp`**，并用 `@` 附上 §1 里的文档。

---

## 提示词（复制从此处开始）

你在继续 **印尼 MiniApp 阶段 1 · 隔离骨架**，不是从零开始，也不是做阶段 2–5。

工作区必须是 `/Users/qj/python_project/IDN_miniapp`（GitHub `linshu368/IDN_miniapp`，分支 `dev`）。  
**禁止**修改、commit 或 push 中文仓 `/Users/qj/python_project/ST_miniAPP`。印尼 commit 不得回中文仓库。

先完整阅读并遵守这些文档（以它们为准，不要靠聊天记忆猜）：

1. `docs/印尼版miniapp开发思路.md` — 已锁定产品决策
2. `docs/印尼版miniapp开发计划.md` — 分阶段计划；当前只做「阶段 1」
3. `docs/印尼版阶段1进度.md` — 已完成项、地址、阻塞、未提交文件

开发口令：copy 独立副本 → mock 明确不做的入口 → 印尼语适配与内容入库 → 真机验收上线。  
本窗口 **只收尾阶段 1**。阶段 2 mock、阶段 3 印尼语、阶段 4 内容入库、Admin/CS 部署都不要开工，除非阶段 1 验收已全部通过且用户明确要求继续。

### 阶段 1 本窗口目标

印尼环境健康可用，且不可能误连中文环境。本窗口要做到：

1. **立刻修** Railway `/api/characters` 500。控制台变量已是正确 pooler（`aws-0-ap-southeast-1.pooler.supabase.com`），但线上进程仍是 2026-09-07 17:03 的部署 `dfd26b99`，还握着旧的 `aws-1` 连接串。执行 `railway redeploy --service backend -y`；若 GitHub 源不可用则 `railway up --service backend -d -y`。用 `Origin: https://idn-miniapp-frontend.vercel.app` 验证接口变为 **200 空列表**。不要导入中文角色数据。
2. 提交并推送到 **本仓** `dev`：根目录 `Dockerfile`、`.dockerignore`、`.gitignore` 的 `.local-idn/` 规则；进度/思路/计划文档可一并提交。未获用户明确要求不要 push。不要提交 `.local-idn/`。
3. 打通或记录发布方式：Railway GitHub App 目前看不到私有仓；Vercel 生产分支仍是 `main`（`main` 还停在基线 `856329d`，隔离提交 `c3b12b7` 只在 `dev`）。在用户同意前，不要把隔离提交强推到 `main`。
4. 用印尼 Bot `@mijing_idn_bot` 做最小冒烟：打开 Mini App、`GET /health`、空角色列表、CORS。`X-Init-Data` 真机鉴权尽量做；库是空的，大厅无卡是预期。
5. 阶段 1 计划里的 Admin/CS 本轮已口头确认 **后做**，不要新建中文 Admin/CS 的指向，也不要部署支付 Worker/Cron。`PAYMENT_ENABLED` 保持 `false`。

### 硬约束

- 不要改中文现网；不要复用中文 Telegram token / 中文 Supabase / 中文 Redis。
- 密钥只读写 `.local-idn/bootstrap.env`（已 gitignore）。回复里不要打印 token、密码、JWT、连接串。
- 回复用户时不要复述连接串或密钥。
- C1/C4/C5/C6/C8 未拍板，不挡阶段 1；C7 已定为保留四 Tab。
- 前端构建缺 `NEXT_PUBLIC_API_URL` 必须失败，禁止加回中文 Railway 回落 URL。
- `railway-pr-env.yml` 保持关闭（里面仍有中文 Railway 项目 ID）。
- 结构已从中文 **test** 库 schema-only 导入印尼空库；不要重放 `packages/shared/migrations/001` 起的迁移，也不要迁用户/订单/会话。
- 默认头像 URL 仍指向中文 test Storage，阶段 1 可先记下，不要大改 shared。

### 验收标准（阶段 1）

- `GET https://backend-production-2e76.up.railway.app/health` → 200
- `GET /api/characters`（带印尼前端 Origin）→ 200 且空列表，不再出现 `tenant/user ... not found`
- 前端 https://idn-miniapp-frontend.vercel.app 可打开
- 服务只连印尼 Supabase `bklxvrrtccphrtnpqulh`，不连中文项目
- 进度文档已更新为「阶段 1 骨架剩余项」的最新状态

做完先更新 `docs/印尼版阶段1进度.md`，再向用户汇报。需要 commit / push / 改 Vercel 生产分支 / 授权 Railway GitHub 时，先问用户。

## 提示词（复制到此处结束）
