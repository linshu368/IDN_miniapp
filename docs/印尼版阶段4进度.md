# 印尼版 MiniApp · 阶段 4 进度

> 记录截止 **2026-09-08 12:45 (UTC+8)**。后续工作只在本仓库进行，不要改中文仓 `ST_miniAPP`。
>
> 锁定方案：`docs/印尼版miniapp开发思路.md`、`docs/印尼版miniapp开发计划.md`。
> 本阶段目标：**内容、经济配置与资源入库**；不部署 Admin / CS。
>
> 用户拍板（2026-09-08）：
>
> 1. 229 张角色卡全部留在大厅，**不**改 `enabled=false`；`system_prompt` **不**译成中文/印尼语，印尼语输出靠 `system_instructions`。
> 2. C1「星尘」用户侧译名 = **Binar**。
> 3. 无充值策略按建议：注册 398、每卡免费额度 40、每日签到 40、至少一个 `is_free=true` 模型、篇幅按 kata。
> 4. 邀请按建议：`invitee_registered` / `invitee_chat_rounds` 开，`invitee_first_paid` 关，单关系上限 400，然后打开入口。
> 5. 不部署 Admin；config 直写印尼 Supabase，后续也在 Supabase 改。
> 6. CS 工作台完全不管；C 端客服开场白印尼语；官方公告留空。

当前分支：**`main`**（未提交）。阶段 1 发布分支约定不变。

---

## 1. 一句话状态

阶段 4 的 **Supabase 配置与邀请 RPC 已经写进印尼库**。大厅仍是 **229/229 启用卡**。C 端货币名、邀请上限展示、平台规则模板和客服开场白已在本仓库改成 **Binar / 印尼语**。

代码还没 commit / 没发 Vercel 与 Railway。现网前端仍是中文壳 + 写死「2200星尘」；库里邀请入口已经打开，**发版前现网「我的」可能露出错误的 2200 文案**。

---

## 2. 印尼 Supabase 已写入的配置

项目 ref：`bklxvrrtccphrtnpqulh`。写入脚本：`ops/idn/apply-runtime-config.py`（拒绝非该 ref 的 URL）。配置包：`ops/idn/runtime-config.json`。

| key                                       | 值                                                                                                           |
| ----------------------------------------- | ------------------------------------------------------------------------------------------------------------ |
| `miniapp_new_user_signup_bonus_credits`   | 398                                                                                                          |
| `miniapp_daily_checkin_bonus_credits`     | 40                                                                                                           |
| `miniapp_character_free_chat_quota_limit` | 40                                                                                                           |
| `llm_model_catalog`                       | 默认 `gemini-flash-lite`（`is_free=true`）；DeepSeek V3.2 / Gemini 3.1 Pro 付费                              |
| `llm_pricing_config`                      | 10 / 15 / 30 / 50                                                                                            |
| `pref_word_count_tiers`                   | 档位 id 不变；`prompt_value` 为 50–150 / 150–250 / 250–400 / 400+ kata；默认 `300-500`                       |
| `system_instructions`                     | 印尼语；强制 Bahasa Indonesia；含 `{{WORD_COUNT}}` / `{{INTERACTION_MODE}}` / `{{USER_CUSTOM_INSTRUCTIONS}}` |
| `miniapp_invite_entry_enabled`            | `true`                                                                                                       |
| `miniapp_invite_reward_rules`             | cap **400**；registered 200 开；chat_rounds 200 / 3 轮开；first_paid 关                                      |
| `miniapp_invite_center_config`            | 印尼语 `{link}` 模板两条；`poster_url` 空                                                                    |
| `lobby_pinned_characters`                 | 空（不钉）                                                                                                   |
| `system_fallback_character_id`            | `ba658771-277a-42e6-8974-e0e662611f4c`（Pesan Dalam Botol-Peluang Romantis，enabled）                        |

`app_core.runtime_config` 现为 **16** 行。`miniapp_features.notifications` **0 行**（公告留空）。

角色：`enabled=229 / total=229`。线上 `GET /api/characters` 仍返回 229。

---

## 3. 邀请 RPC（只改印尼库）

中文 108 之后 `bind_invite` 不再发 `invitee_registered`，且 `check_invite_chat_rounds_reward` 把已发的 registered 当成「轮次奖已发」。只开 config 开关不够。

已用 `ops/idn/patch-invite-registered.sql` 打到印尼库：

- `bind_invite` 成功绑定后 `grant_invite_reward(..., 'invitee_registered', ...)`（RPC 仍按规则 `enabled` 决定是否真发）。
- `check_invite_chat_rounds_reward` 只按 `invitee_chat_rounds` 去重，与注册奖独立。

核对：`bind_invite` 含 registered grant；chat_rounds 去重不再扫 `invitee_registered`。

---

## 4. 代码侧（本仓库，未发版）

- `packages/shared/src/market-locale.ts`：`CREDITS_NAME = 'Binar'`
- 「我的」邀请卡、邀请中心规则文案读 `total_cap_credits` / `chat_rounds_threshold`，不再写死 2200
- 充值 / 订单 / 许愿 / 余额不足 / 消费明细等用户可见「kredit」改为 Binar
- 平台规则兜底模板与 `wrapUserInput` 改为印尼语包装；`{{user}}` 空名回落 `kamu`
- C 端客服开场白：`Halo, ini bantuan PribadiAI. Ceritakan masalahmu, kami akan membalas secepatnya.`
- **未**改 Admin UI、**未**改 CS 工作台、**未**部署 Admin/CS

默认头像：中文站原图 2.3MB，印尼桶上限 2 MiB。已用 `sips` 压到 512px / 约 447KB 后上传同一路径；公开 URL 可访问。用户自行上传仍由后端 `MAX_AVATAR_BYTES = 2MB` 限制。

---

## 5. 对照计划验收表

| 项                                     | 本轮                                          |
| -------------------------------------- | --------------------------------------------- |
| 角色卡 enabled / system_prompt         | 229 全开；prompt 未改写；输出靠 instruction   |
| runtime_config 经济 + 模型 + kata 档位 | 已写入印尼库                                  |
| 邀请规则 + 入口                        | 已写入；RPC 已补丁                            |
| 去掉写死 2200                          | 本仓库已改；**现网前端未发版，仍是 2200星尘** |
| 官方公告                               | 不写，表空                                    |
| C 端客服开场白                         | 印尼语                                        |
| Admin / CS                             | 不部署、不接入                                |

---

## 6. 本轮命令结果

```text
python3 ops/idn/apply-runtime-config.py          # 16 keys upsert 成功；头像因体积跳过
python3 ops/idn/apply-runtime-config.py --sql-only
  # bind_invite + check_invite_chat_rounds_reward patched；runtime_config rows=16

pnpm --filter @miniapp/frontend test             # 15 files / 69 tests passed
pnpm --filter @miniapp/frontend typecheck        # 通过
pnpm --filter @miniapp/frontend lint             # No ESLint warnings or errors

pnpm --filter @miniapp/backend test -- \
  src/lib/invite-config.test.ts \
  src/features/billing/free-quota.test.ts \
  src/features/engine/prompt-engine.test.ts \
  src/features/engine/render-instructions.test.ts \
  src/infrastructure/repositories/MiniappWalletRepository.test.ts \
  src/features/engine/platform-instructions.test.ts
  # 6 files / 45 tests passed
```

本地 `next dev`（当前工作树）打开过：

- `/create` → Kreasi 占位 + Lobby/Chat/Kreasi/Saya
- `/profile/recharge` → **Isi Binar** + Check-in harian / Undang teman，无支付按钮
- `/profile/orders` → Pesanan 占位
- `/profile` → Saldo Binar / Isi Binar（无 Telegram 鉴权时邀请卡不出现）
- `/profile/support` → 开场白印尼语
- `/profile/invite` → Pusat undangan
- `/profile/messages` → Resmi / Pesan，无公告正文

大厅 229 张卡由线上 `GET /api/characters` 与库表核对，浏览器未登录进不了真机大厅。

---

## 7. 发版前必做

1. **先发前端 + 后端，再依赖现网邀请入口。** 库开关已是 `true`；现网前端仍写死「2200星尘」。后端新字段 `total_cap_credits` / `chat_rounds_threshold` 也要 Railway 发版后，「我的」才会显示 **400 Binar**。
2. 模型目录 / instruction 缓存在后端约 5 分钟，配置已在库里，旧后端进程会自己读到新 config；邀请入口展示数字仍取决于前端是否发版。
3. ~~缩小默认头像后上传到印尼 Storage。~~ **已完成**：512px / ~447KB，公开 URL 可访问。
4. 真机用两个 Telegram 账号走邀请绑定：注册 200 + 聊满 3 轮再 200，付费奖保持关闭。
5. 不要从中文仓再 copy 配置；改经济数字直接改印尼 `app_core.runtime_config`。

未提交、未 push。`.local-idn/` 继续 gitignore。
