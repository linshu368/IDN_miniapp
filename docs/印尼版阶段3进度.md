# 印尼版 MiniApp · 阶段 3 进度

> 记录截止 **2026-09-08 20:40 (UTC+8)**。后续工作只在本仓库进行，不要改中文仓 `ST_miniAPP`。
>
> 锁定方案：`docs/印尼版miniapp开发思路.md`、`docs/印尼版miniapp开发计划.md`。
> 本阶段目标：**所有 C 端用户可见内容是印尼语；运营台和 CS 工作台 UI 保持中文。**

当前分支：**`main`**（未提交）。阶段 1 发布分支约定不变。

---

## 1. 一句话状态

阶段 3 的 C 端印尼语适配 **已经落地**：`lang="id"`、底栏、大厅 / 聊天 / 我的、时间与截断、用户可见 API message、Bot 简介与命令均已换成印尼语。货币名 C1 锁为 **Binar**（单一常量 `CREDITS_NAME`）。Admin / CS UI 未翻译。

代码还没 commit / 没发 Vercel 与 Railway。现网前端仍是中文壳。

---

## 2. 本轮拍板

| #        | 结论                                                                                                                         |
| -------- | ---------------------------------------------------------------------------------------------------------------------------- |
| C1       | 用户侧「星尘」= **Binar**。前端、后端兜底文案、邀请页、Bot 简介共用 `packages/shared/src/market-locale.ts` 的 `CREDITS_NAME` |
| 口吻     | 非正式 `kamu`，不用 `Anda`                                                                                                   |
| 截断     | 角色名 / 会话标题 16 grapheme + 省略号，不再用中文 7 字                                                                      |
| 时区     | 展示时间固定 `Asia/Jakarta`（WIB，UTC+7），locale `id-ID`                                                                    |
| Bot      | 用 Bot API 设命令、菜单按钮、简介；**隐私政策链接本阶段不改**                                                                |
| 邀请数字 | 不写死 2200；壳文案走配置字段或无金额表述                                                                                    |

---

## 3. 对照计划验收表

| 表面           | 副本改动                                                                         | 本轮验证                                                                               |
| -------------- | -------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------- |
| 根壳与导航     | `layout.tsx` / `global-error.tsx` `lang="id"`；底栏 Lobby / Chat / Kreasi / Saya | 本地打开主壳：四 Tab 印尼语，`html lang=id`                                            |
| 大厅与角色详情 | 搜索、排序、空态、详情、收藏、进房 CTA                                           | 代码路径已换；大厅角色内容属阶段 4                                                     |
| 聊天           | 输入、SSE 错误、重生成、会话抽屉、模型 / 生成偏好、免费额度、mock 提示           | 单测覆盖流式 402 文案走 `CREDITS_NAME`                                                 |
| 我的与引流     | 资料、签到、钱包、邀请、消费明细、客服、消息中心                                 | 本地打开过「我的」/ 邀请 / 消息 / 客服（阶段 4 文档亦有记录）                          |
| 时间与格式     | `formatWhisperTime` / 消息时间 / 订单分组用 Jakarta；数字 `id-ID`                | 单测：`zonedDateParts`、`formatMessageTime`                                            |
| 用户可见 API   | `fail()`、会话 `DEFAULT_MESSAGE`、`status_label`、支付/额度兜底；不引导充值      | 钱包 `formatSpendingStatus` 单测已改                                                   |
| Telegram Bot   | `/start` 命令、简介、短简介、菜单按钮                                            | API：`start` = Buka Mini App PribadiAI；菜单 **Mulai**；简介已改为 Binar。隐私链接未改 |

不翻译：代码注释、Admin UI、CS 工作台、`cs-platform` 路由。

---

## 4. 关键代码入口

| 文件                                                                          | 作用                                                                         |
| ----------------------------------------------------------------------------- | ---------------------------------------------------------------------------- |
| `packages/shared/src/market-locale.ts`                                        | C1 `CREDITS_NAME = 'Binar'`、`DISPLAY_LANG` / `DISPLAY_TIME_ZONE` / 截断上限 |
| `packages/frontend/src/lib/locale.ts`                                         | Jakarta 日历拆分、`truncateDisplayName`                                      |
| `packages/frontend/src/lib/market-copy.ts`                                    | 阶段 2 占位页 / 余额不足弹窗，货币名读 `CREDITS_NAME`                        |
| `packages/backend/src/features/conversations/errors.ts`                       | 会话错误用户文案                                                             |
| `packages/backend/src/infrastructure/repositories/MiniappWalletRepository.ts` | 消费明细 `status_label`                                                      |

---

## 5. Bot（`@mijing_idn_bot` / PribadiAI）

已用 Bot API 写入（默认语言 + `language_code=id`）：

- 命令：`/start` → Buka Mini App PribadiAI
- 菜单按钮：`Mulai`（Web App URL 仍是印尼 Vercel）
- Description / Short description：印尼语，货币名 **Binar**
- 未改：BotFather 隐私政策链接、Bot 显示名（已是 PribadiAI）

Webhook 仍不自动回复 `/start` 欢迎语；用户靠菜单按钮进 Mini App。

---

## 6. 本轮命令结果

```text
pnpm --filter @miniapp/frontend test       # 15 files / 69 tests passed
pnpm --filter @miniapp/frontend typecheck  # tsc --noEmit 通过
pnpm --filter @miniapp/frontend lint       # No ESLint warnings or errors
pnpm --filter @miniapp/shared test         # 8 files / 62 tests passed
pnpm --filter @miniapp/backend exec vitest run \
  src/infrastructure/repositories/MiniappWalletRepository.test.ts \
  src/features/billing/free-quota.test.ts
  # 2 files / 7 tests passed
```

本地 `next dev` 打开过（与阶段 4 工作树重叠）：

- `/` 底栏四列 Lobby / Chat / Kreasi / Saya
- `/create` → Kreasi 占位
- `/profile` → Saldo Binar / Isi Binar
- `/profile/recharge` → Isi Binar 占位，无支付按钮

---

## 7. 明确不做 / 留给阶段 4–5

- runtime_config、角色卡正文、Storage、无充值经济数字 → 阶段 4（配置已开始写入印尼库，见 `docs/印尼版阶段4进度.md`）
- 真机扫中文残留、邀请双账号、SSE → 阶段 5 / 发版后
- 隐私政策 URL
- Admin / CS 独立部署

---

## 8. 建议的下一步

1. 发前端 + 后端。现网仍是中文壳；阶段 4 已打开邀请入口，不发版会露出旧的「2200星尘」。
2. 真机走大厅 / 聊天 / 签到 / 邀请 / 客服，扫 C 端中文残留。
3. 阶段 1 遗留、不挡发版：独立 `SENTRY_*`、`.railway/railway.ts` `config apply`。
